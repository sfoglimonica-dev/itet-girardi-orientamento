


{
  "version": 2,
  "buildCommand": "cd SchoolOrientation && npm install && npm run build",
  "outputDirectory": "SchoolOrientation/dist/public",
  "installCommand": "cd SchoolOrientation && npm install",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
